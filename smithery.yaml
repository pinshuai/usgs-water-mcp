startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |
    (config) => ({
      "command": "python",
      "args": ["-m", "usgs_water_mcp"],
      "env": {}
    })

build:
  dockerBuildPath: .